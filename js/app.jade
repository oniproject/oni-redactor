div
	.area(v-repeat="areas" style="left: {{x}}px; top: {{y}}px; width: {{w}}px; height: {{h}}px;")
		.split.horizontal(v-on="click: split($index, 'horizontal')")
		.split.vertical(v-on="click: split($index, 'vertical')")
		div {{$index}}

	div(v-repeat="handlers" v-component="handler")
